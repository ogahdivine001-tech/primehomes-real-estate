# Copy this file to .env and fill in your values from sanity.io/manage
# (see studio/README.md for the full setup walkthrough).
#
# Leave these blank / delete .env entirely to run the site with the
# built-in sample data instead of a live Sanity connection.

VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
